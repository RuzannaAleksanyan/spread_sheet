# Spread Sheet Program

This is a simple spreadsheet program implemented in C++. It allows you to create and manage a spreadsheet with cells of different types, including integers, doubles, and strings.

## Features

- Create a spreadsheet with specified dimensions (rows and columns).
- Set and retrieve cell values of different types: integers, doubles, and strings.
- Add rows and columns to the spreadsheet dynamically.
- Supports basic error handling for out-of-range cell access.

## Usage

1. Clone the repository or download the source code.

2. Build the program using a C++ compiler. For example, you can use `g++`:

   g++ main.cpp spreadSheet.cpp -o spreadsheet
